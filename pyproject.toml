[project]
name = "skunkworks"
version = "1.0.9"
description = "Policy-controlled mission control and fleet automation for the Von Neumann Game"
readme = "README.md"
authors = [{ name = "Christopher Ziegenhagel (Ziegenbagel)", email = "ziegenbagel.gaming@gmail.com" }]
license = "GPL-3.0-only"
requires-python = ">=3.14"
keywords = ["desktop", "fleet-automation", "mission-control", "pyside6", "qt"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: X11 Applications :: Qt",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "pyside6>=6.11.1",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "keyring>=25.6.0",
    "tzdata>=2025.2",
]

[project.scripts]
skunkworks = "src.ui.app:run"

[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["src", "src.*"]

[tool.setuptools.package-data]
"src.ui" = ["qml/**/*", "assets/**/*"]

[project.urls]
Homepage = "https://github.com/Ziegenbagel/skunkworks"
Releases = "https://github.com/Ziegenbagel/skunkworks/releases"
Issues = "https://github.com/Ziegenbagel/skunkworks/issues"

[dependency-groups]
dev = [
    "pillow>=12.3.0",
    "pytest>=9.1.1",
    "pytest-qt>=4.5.0",
]

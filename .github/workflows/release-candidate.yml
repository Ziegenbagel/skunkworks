name: Build unsigned release candidates

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version label for candidate artifacts
        required: true
        default: next-dev

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            platform: macOS-arm64
          - os: macos-15-intel
            platform: macOS-x86_64
          - os: windows-latest
            platform: Windows-x64
          - os: ubuntu-22.04
            platform: Linux-x86_64
    runs-on: ${{ matrix.os }}
    env:
      SKUNKWORKS_HOME: ${{ github.workspace }}/.skunkworks-release
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip
      - name: Install Linux Qt runtime libraries
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install --yes libegl1 libgl1 libxkbcommon-x11-0
      - name: Install project and deployment dependencies
        run: python -m pip install -e . pytest pytest-qt pillow pyinstaller==6.22.2 imageio==2.37.4
      - name: Verify candidate source
        run: python -m pytest -q tests
      - name: Verify release contents
        run: python -m tools.release_readiness
      - name: Build standalone application
        run: python -m tools.build_release_candidate --clean
      - name: Add release documents
        shell: bash
        run: |
          mkdir -p build/release-candidate/release-documents
          cp LICENSE NOTICE PRIVACY.md SECURITY.md SUPPORT.md THIRD_PARTY_NOTICES.md RELEASE_NOTES.md build/release-candidate/release-documents/
          python -m tools.collect_release_licenses build/release-candidate/release-documents
      - name: Build curated source archive
        shell: bash
        run: git archive --format=zip --output=build/release-candidate/Skunkworks-${{ inputs.version }}-source.zip HEAD
      - name: Audit complete candidate for private data
        run: python -m tools.audit_release_tree build/release-candidate
      - name: Upload unsigned candidate
        uses: actions/upload-artifact@v4
        with:
          name: Skunkworks-${{ inputs.version }}-${{ matrix.platform }}-unsigned
          path: build/release-candidate
          if-no-files-found: error

# These artifacts are deliberately not attached to a public GitHub Release.
# Platform signing, notarization, installer creation, checksums, and clean-machine
# acceptance must succeed before a separate publication workflow is enabled.
